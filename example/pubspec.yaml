name: app_help_center_example
description: Example app for app_help_center.
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  app_help_center:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
